criar novo projeto react 
### npx create-react-app my-app
### cd my-app
### npm start
rfc pour creer le composant automatiquement 
npm install broostrap  afim d'installer le broostrap
emmet pour completer automatimaquement le class ou div 
rodar o projeto 

### npm start
Gerenciar as rotas 

### npm install react-router-dom
 transforma o css em componentes
### npm install --save styled-components



####  token hostgastor
NDBASX1JVWMIQYRIGO4JEZSTYCD8V4QC

#### npm run build

### {showModalLogin && <Login func={toggleModalLogin} funcRegister={toggleModalRegister} />}
 ###     {showModalRegister && <RegisterUser func={toggleModalLogin()} funcRegister={toggleModalRegister} />}
 ### const [showModalLogin, setShowModalLogin] = useState(false);

 ####
   const toggleModalLogin = () => {
    setShowModal(false);
    SetIsMobile(false)
    setShowModalPesquisa(false)
    setShowModalRegister(false);
    setShowModalLogin(!showModalLogin)
  };

  # installar reduz e react redux
  
  # npm install redux 
  # npm install react-redux
  # npm install redux-thunk

  # npm install uuid 